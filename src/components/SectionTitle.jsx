const SectionTitle = ({ text }) => {
  return (
    <div className="pb-4 border-b border-base-300">
      <h2 className="font-medium text-3xl capitalize tracking-wider">{text}</h2>
    </div>
  )
}
export default SectionTitle
